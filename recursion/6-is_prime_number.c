#include "main.h"

/**
 * is_prime_number - finds whether input is prime number
 * @n: number to check if prime
 *
 * Return: 1 if number is prime, otherwise 0
 */

int is_prime_number(int n)
{
	if (n <= 0)
	{
		return (0);
	}
	else if (n == 1)
	{
		return (1);
	}
	else (
}
